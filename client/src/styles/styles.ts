import { classify } from '../shared/Utills/commonUtils';

export const themeBase = classify({
  base: 'bg-white dark:bg-gray-800',
});

export const textBase = classify({
  base: 'text-base text-gray-900 dark:text-white',
});
